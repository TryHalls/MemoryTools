Close.Font = Enum.Font.GothamBold
Close.Text = "×"
Close.TextColor3 = Color3.new(1, 1, 1)
Close.TextSize = 20
Instance.new("UICorner", Close).CornerRadius = UDim.new(0, 7)

local Content = Instance.new("Frame", Main)
Content.BackgroundTransparency = 1
Content.Position = UDim2.new(0, 10, 0, 54)
Content.Size = UDim2.new(1, -20, 1, -64)

local Left = Instance.new("ScrollingFrame", Content)
Left.Size = UDim2.new(0.34, -5, 1, 0)
Left.BackgroundColor3 = Color3.fromRGB(26, 29, 35)
Left.BorderSizePixel = 0
Left.ScrollBarThickness = 4
Left.CanvasSize = UDim2.new(0, 0, 0, 760)
Instance.new("UICorner", Left).CornerRadius = UDim.new(0, 9)

local Right = Instance.new("Frame", Content)
Right.Position = UDim2.new(0.34, 5, 0, 0)
Right.Size = UDim2.new(0.66, -5, 1, 0)
Right.BackgroundColor3 = Color3.fromRGB(13, 15, 18)
Right.BorderSizePixel = 0
Instance.new("UICorner", Right).CornerRadius = UDim.new(0, 9)

local function label(text, y, h, bold)
    local l = Instance.new("TextLabel", Left)
    l.BackgroundTransparency = 1
    l.Position = UDim2.new(0, 12, 0, y)
    l.Size = UDim2.new(1, -24, 0, h)
    l.Font = bold and Enum.Font.GothamBold or Enum.Font.Gotham
    l.Text = text
    l.TextColor3 = bold and Color3.fromRGB(230,233,239) or Color3.fromRGB(155,162,175)
    l.TextSize = bold and 13 or 11
    l.TextWrapped = true
    l.TextXAlignment = Enum.TextXAlignment.Left
    l.TextYAlignment = Enum.TextYAlignment.Top
    return l
end

local function button(text, y, color)
    local b = Instance.new("TextButton", Left)
    b.Position = UDim2.new(0, 12, 0, y)
    b.Size = UDim2.new(1, -24, 0, 40)
    b.BackgroundColor3 = color or Color3.fromRGB(55,92,168)
    b.BorderSizePixel = 0
    b.Font = Enum.Font.GothamBold
    b.Text = text
    b.TextColor3 = Color3.fromRGB(245,247,251)
    b.TextSize = 12
    Instance.new("UICorner", b).CornerRadius = UDim.new(0, 8)
    return b
end

label("ÚLTIMO PASE", 12, 22, true)
label("Cierra Auto Steal + Player. No invoca remotes ni require(); solo estructura runtime y decompilado dirigido.", 40, 68, false)
local ScanButton = button("INICIAR FINAL SCAN", 114)
local SourceButton = button("ANALIZAR FINAL SOURCE", 160, Color3.fromRGB(52,56,66))
SourceButton.AutoButtonColor = false
SourceButton.TextColor3 = Color3.fromRGB(122,128,139)

label("EXPORTAR", 216, 20, true)
local CopyFinal = button("COPIAR FINAL MAP", 242, Color3.fromRGB(52,56,66))
local CopyRuntime = button("COPIAR RUNTIME MAP", 288, Color3.fromRGB(52,56,66))
local CopySource = button("COPIAR SOURCE MAP", 334, Color3.fromRGB(52,56,66))
local CopySummary = button("COPIAR RESUMEN", 380, Color3.fromRGB(52,56,66))
local NextPart = button("SIGUIENTE PARTE", 426, Color3.fromRGB(80,73,124))
NextPart.Visible = false

label("ESTADO", 482, 20, true)
local Status = label("Listo.", 508, 60, false)
local ProgressBack = Instance.new("Frame", Left)
ProgressBack.Position = UDim2.new(0,12,0,574)
ProgressBack.Size = UDim2.new(1,-24,0,8)
ProgressBack.BackgroundColor3 = Color3.fromRGB(42,46,55)
ProgressBack.BorderSizePixel = 0
Instance.new("UICorner", ProgressBack).CornerRadius = UDim.new(1,0)
local ProgressFill = Instance.new("Frame", ProgressBack)
ProgressFill.Size = UDim2.fromScale(0,1)
ProgressFill.BackgroundColor3 = Color3.fromRGB(91,139,231)
ProgressFill.BorderSizePixel = 0
Instance.new("UICorner", ProgressFill).CornerRadius = UDim.new(1,0)
label("Orden: FINAL SCAN → FINAL SOURCE → COPIAR FINAL MAP. Ese reporte debe ser el último antes de programar V1.", 604, 86, false)

local Header = Instance.new("TextLabel", Right)
Header.BackgroundColor3 = Color3.fromRGB(21,24,29)
Header.BorderSizePixel = 0
Header.Size = UDim2.new(1,0,0,34)
Header.Font = Enum.Font.Code
Header.Text = "  TERMINAL INTERNA"
Header.TextColor3 = Color3.fromRGB(171,222,160)
Header.TextSize = 13
Header.TextXAlignment = Enum.TextXAlignment.Left

local Scroll = Instance.new("ScrollingFrame", Right)
Scroll.Position = UDim2.new(0,8,0,42)
Scroll.Size = UDim2.new(1,-16,1,-50)
Scroll.BackgroundTransparency = 1
Scroll.BorderSizePixel = 0
Scroll.ScrollBarThickness = 5
Scroll.AutomaticCanvasSize = Enum.AutomaticSize.Y
Scroll.CanvasSize = UDim2.new()

local Terminal = Instance.new("TextBox", Scroll)
Terminal.Size = UDim2.new(1,-8,0,0)
Terminal.AutomaticSize = Enum.AutomaticSize.Y
Terminal.BackgroundTransparency = 1
Terminal.Font = Enum.Font.Code
Terminal.MultiLine = true
Terminal.ClearTextOnFocus = false
Terminal.TextEditable = false
Terminal.Text = "[READY] V1.6 listo.\nPulsa INICIAR FINAL SCAN."
Terminal.TextColor3 = Color3.fromRGB(200,205,214)
Terminal.TextSize = 12
Terminal.TextWrapped = true
Terminal.TextXAlignment = Enum.TextXAlignment.Left
Terminal.TextYAlignment = Enum.TextYAlignment.Top

local function terminalSet(text)
    Terminal.Text = tostring(text)
    task.defer(function() Scroll.CanvasPosition = Vector2.new(0,0) end)
end

local function terminalAdd(text)
    Terminal.Text = Terminal.Text .. "\n" .. tostring(text)
    task.defer(function() Scroll.CanvasPosition = Vector2.new(0, math.max(0, Scroll.AbsoluteCanvasSize.Y)) end)
end

local function setStatus(text, progress)
    Status.Text = tostring(text)
    if progress ~= nil then
        ProgressFill.Size = UDim2.fromScale(math.clamp(progress,0,1),1)
    end
end

local dragging, dragStart, startPosition = false, nil, nil
Top.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        dragging = true
        dragStart = input.Position
        startPosition = Main.Position
    end
end)
UserInputService.InputChanged:Connect(function(input)
    if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
        local delta = input.Position - dragStart
        Main.Position = UDim2.new(startPosition.X.Scale, startPosition.X.Offset + delta.X, startPosition.Y.Scale, startPosition.Y.Offset + delta.Y)
    end
end)
UserInputService.InputEnded:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then dragging = false end
end)
Close.MouseButton1Click:Connect(function() ScreenGui:Destroy() end)

-- State -----------------------------------------------------------------------

local function emptySource()
    return {
        attempted = false,
        targets = {},
        missing = {},
        results = {},
        failures = {},
        sourceChars = 0,
        contexts = 0,
    }
end

local function emptyState()
    return {
        total = 0,
        seen = {},
        modules = {},
        scripts = {},
        roots = {},
        warnings = {},
        runtimeHits = {},
        runtimeHitSeen = {},
        humanoid = {},
        source = emptySource(),
    }
end

local state = emptyState()
local reports = nil
local running = false
local sourceRunning = false
local activeChunks, activeIndex, activeName = nil, 1, nil

local function ownGui(instance)
    return instance == ScreenGui or safeCall(function() return instance:IsDescendantOf(ScreenGui) end, false)
end

local function pathMatchesOwner(instance)
    local path = string.lower(fullName(instance))
    local name = string.lower(LocalPlayer.Name)
    if path:find(name, 1, true) then return true end
    local attrs = safeCall(function() return instance:GetAttributes() end, {})
    if type(attrs) == "table" then
        for key, value in pairs(attrs) do
            local k = string.lower(tostring(key))
            if k:find("owner",1,true) or k:find("user",1,true) or k:find("player",1,true) then
